// ============================================================================
// credentials.h.example -- Copy to credentials.h (same folder as the .ino)
// and fill in your values before building esp32s3_master.ino in Arduino IDE.
// ============================================================================
#pragma once

#define WIFI_SSID     "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
